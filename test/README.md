# Root test folder

Automated tests live next to their packages:

- Backend: `backend/tests/`
- Frontend: `frontend/tests/`

Use `python3 build.py` → option **12** to run all tests after dependencies are installed.
