# Helper scripts

Most developer operations are available through the root menu:

```bash
python3 build.py
```

Python modules used by that menu live in `scripts/build_lib/`.

Do not run dependency installation scripts non-interactively unless you intend
to modify the system — option 3 always explains and confirms first.
